let numeroIngresado = parseInt(prompt("Ingresar numero a dividor por 2 hasta 10"));
for (let i = 2; i <= 10; i++) {
    let resultado = numeroIngresado / i;
    console.log(numeroIngresado + " / " + i + "=" + resultado);
}

